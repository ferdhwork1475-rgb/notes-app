TODO: 
Limit the data coming from the backend
use the same route and controller - check for searchTerm or queries optionally so it does not throw error on loading
check the createNote, editNote and how tags are being saved and rendered.

get the page number from the frontend end and use the formula to reder the document
get the number of documents skipped
get the length of documents all together
use the length of documents to determine the number of pages
get the current number of documents being rendered (21, 30)